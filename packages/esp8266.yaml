substitutions:
  comment: "ESPHome Svitlo Bot | ESP 8266"

http_request:
  tls_buffer_size_rx: 16384

interval:
  - interval: ${heartbeat_interval}s
    id: svitlobot
    then:
      - script.execute: site_ping
